pipeline {
    agent any

    stages {
        stage('Testing') {
            steps {
                echo 'code is passed'
            }
        }
        stage('Deployment') {
            steps {
                echo 'Code is deployed in server'
            }
        }
        
    }
}
